<h1>Add A New Customer</h1>
<%= form_for @customer do |f| %>
  <%= f.label :name %>
  <%= errors_for(@customer, :name) %>
  <%= f.text_field :name %>
  <%= f.label :phone, "Phone Number" %>
  <%= errors_for(@customer, :phone) %>
  <%= f.text_field :phone %>
  <%= f.submit "Create Customer" %>
<% end %>
